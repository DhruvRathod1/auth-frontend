version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
environment:
  variables:
    REACT_APP_API_BASE_URL: https://7rtfzr1a2i.execute-api.ap-south-1.amazonaws.com/Dev